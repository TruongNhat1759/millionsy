import React, { useEffect, useState } from 'react';
import useStyles from './styles';
type Props = {
  // dataGiveSecond: (getDataSecond: any) => void,
  // dataSendSecond: any,
}
const ViewSubmit: React.FC<Props> = () => {
  const classes = useStyles();

  return (
    <>
      <div>
        test
      </div>
    </>
  )
}

export default ViewSubmit